services:
  relay-mcp:
    image: entirevc/evc-team-relay-mcp:latest
    ports:
      - "8888:8888"
    environment:
      - RELAY_CP_URL=${RELAY_CP_URL}
      - RELAY_EMAIL=${RELAY_EMAIL}
      - RELAY_PASSWORD=${RELAY_PASSWORD}
    restart: unless-stopped
